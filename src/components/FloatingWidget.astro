<!-- ============================================
     Floating Widget — Instagram / Email / WhatsApp
     ============================================ -->

<div class="floating-widget">
  <!-- WhatsApp -->
  <a href="https://wa.me/33744806714" target="_blank" rel="noopener noreferrer" aria-label="WhatsApp" class="floating-widget__link">
    <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="currentColor">
      <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/>
    </svg>
  </a>

  <span class="floating-widget__separator"></span>

  <!-- Email -->
  <a href="mailto:contact@marlenepetit.fr" aria-label="Email" class="floating-widget__link">
    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <rect x="2" y="4" width="20" height="16" rx="2"/>
      <polyline points="22,4 12,13 2,4"/>
    </svg>
  </a>

  <span class="floating-widget__separator"></span>

  <!-- Instagram -->
  <a href="https://www.instagram.com/performeaufeminin/" target="_blank" rel="noopener noreferrer" aria-label="Instagram" class="floating-widget__link">
    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <rect x="2" y="2" width="20" height="20" rx="5" ry="5"/>
      <circle cx="12" cy="12" r="5"/>
      <circle cx="17.5" cy="6.5" r="1.5" fill="currentColor" stroke="none"/>
    </svg>
  </a>
</div>

<style>
  .floating-widget {
    position: fixed;
    bottom: 24px;                                          /* === AJUSTE : distance du bas === */
    right: 24px;                                           /* === AJUSTE : distance de droite (desktop) === */
    z-index: 9999;
    display: flex;
    align-items: center;
    gap: 0;
    background-color: rgba(192, 161, 88, 0.65);             /* === AJUSTE : couleur de fond du widget (avec transparence) === */
    backdrop-filter: blur(8px);                              /* === AJUSTE : flou arrière-plan === */
    -webkit-backdrop-filter: blur(8px);                      /* === AJUSTE : flou arrière-plan Safari === */
    border-radius: 40px;                                   /* === AJUSTE : arrondi de la pilule === */
    padding: 8px 20px;                                     /* === AJUSTE : padding interne (haut/bas | gauche/droite) === */
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.18);           /* === AJUSTE : ombre portée === */
  }

  .floating-widget__link {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 56px;                                           /* === AJUSTE : largeur zone cliquable === */
    height: 40px;                                          /* === AJUSTE : hauteur zone cliquable === */
    color: #ffffff;
    border-radius: 40px;                                   /* === AJUSTE : arrondi du bouton au survol === */
    transition: background-color 0.3s ease;
    text-decoration: none;
  }

  .floating-widget__separator {
    width: 1px;                                            /* === AJUSTE : épaisseur du séparateur === */
    height: 24px;                                          /* === AJUSTE : hauteur du séparateur === */
    background-color: rgba(255, 255, 255, 0.4);            /* === AJUSTE : couleur/opacité du séparateur === */
    flex-shrink: 0;
  }

  .floating-widget__link:hover {
    background-color: rgba(192, 161, 88, 1);               /* === AJUSTE : fond or opacité 1 au survol === */
  }

  /* ---- Desktop : vertical, en bas à droite ---- */
  @media (min-width: 769px) {
    .floating-widget {
      flex-direction: column;
      padding: 20px 8px;                                   /* === AJUSTE : padding interne desktop === */
    }
    .floating-widget__link {
      width: 40px;                                         /* === AJUSTE : largeur zone cliquable desktop === */
      height: 48px;                                        /* === AJUSTE : hauteur zone cliquable desktop === */
    }
    .floating-widget__separator {
      width: 24px;                                         /* === AJUSTE : largeur séparateur desktop === */
      height: 1px;                                         /* === AJUSTE : épaisseur séparateur desktop === */
    }
  }

  /* ---- Mobile : centré en bas, plus large, plus espacé ---- */
  @media (max-width: 768px) {
    .floating-widget {
      right: auto;
      left: 50%;
      transform: translateX(-50%);
      bottom: 16px;                                        /* === AJUSTE : distance du bas mobile === */
      padding: 6px 28px;                                   /* === AJUSTE : padding interne mobile === */
    }
    .floating-widget__link {
      width: 72px;                                         /* === AJUSTE : largeur zone cliquable mobile === */
      height: 38px;                                        /* === AJUSTE : hauteur zone cliquable mobile === */
    }
    .floating-widget__separator {
      height: 28px;                                        /* === AJUSTE : hauteur séparateur mobile === */
      margin: 0 4px;                                       /* === AJUSTE : espacement autour du séparateur mobile === */
    }
  }
</style>
